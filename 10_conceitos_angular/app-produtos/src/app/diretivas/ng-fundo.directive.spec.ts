import { NgFundoDirective } from './ng-fundo.directive';

describe('NgFundoDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFundoDirective();
    expect(directive).toBeTruthy();
  });
});
